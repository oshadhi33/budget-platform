grafana:
  enabled: true

  adminUser: admin
  adminPassword: prom-operator

  service:
    type: ClusterIP

  persistence:
    enabled: false

prometheus:
  prometheusSpec:
    retention: 2d

    resources:
      requests:
        memory: 400Mi

alertmanager:
  enabled: false

nodeExporter:
  enabled: true
